# PostgreSQL 实例设计审查报告

- 实例: `<host>:<port>`
- 扫描时间: `<timestamp>`
- 扫描数据库数: `<N>`
- 扫描范围: 命名规范 / 字段类型 / 注释缺失 / 大表分区 / 索引设计 / 约束默认值 / 库级配置

---

## 一、问题总览表

| 数据库 | 🔴 高危 | 🟡 警告 | 🔵 建议 | 健康评分 |
|---|---|---|---|---|
| db_a | 3 | 12 | 25 | 62 |
| db_b | 0 | 2 | 4 | 92 |
| ... | | | | |
| **合计** | | | | |

评分规则：100 分起，🔴 每项 -10，🟡 每项 -3，🔵 每项 -1，下限为 0。

---

## 二、按数据库详细问题清单

### 数据库：`<db_name>`

> 若本库字段注释缺失率 > 30%，在此处标注：⚠️ **注释缺失率 XX%，已超过 30% 阈值**

#### 🔴 高危风险

| Schema | 对象 | 问题描述 | 当前情况 | 潜在风险 | 修复建议 |
|---|---|---|---|---|---|
| public | orders | 缺少主键 | 表无 PRIMARY KEY 约束 | 无法保证行唯一性，逻辑复制/CDC 受限 | 补建自增或业务唯一主键，并评估存量重复数据 |

#### 🟡 警告风险

| Schema | 对象 | 问题描述 | 当前情况 | 潜在风险 | 修复建议 |
|---|---|---|---|---|---|
| public | orders.user_id | 外键未建索引 | fk 列 user_id 无覆盖索引 | join/级联操作全表扫描 | `CREATE INDEX CONCURRENTLY` 补建索引 |

#### 🔵 建议优化

| Schema | 对象 | 问题描述 | 当前情况 | 潜在风险 | 修复建议 |
|---|---|---|---|---|---|
| public | tmp_test1 | 命名不规范 | 表名含 test/纯数字特征 | 无语义，协作成本高 | 按业务含义重命名或归档清理 |

（其余数据库按相同结构重复）

---

## 三、权限不足 / 需人工复核项

| 数据库 | 检查项 | 状态 | 说明 |
|---|---|---|---|
| db_a | 未使用索引统计 | 需人工复核 | pg_stat 自上次 reset (`<date>`) 不足7天，结果可能失真 |
| db_b | 数据校验和 | 权限不足 | 当前用户无 SHOW 权限，需授予 pg_monitor 角色或等效权限 |

---

## 四、按优先级排序的整改建议 Top 10

1. [🔴] `db_a.public.orders` 补建主键 —— 影响数据正确性，建议本周内完成
2. [🔴] `db_a.public.payment` 金额字段 float → numeric —— 涉及资金精度，需评估迁移窗口
3. ...
（按扣分权重 × 影响面综合排序，高危优先，同级别按所属库健康评分从低到高排序）

---

## 附注

- 本次扫描仅执行只读查询（`information_schema` / `pg_catalog` / `pg_stat_*`），未执行任何 DDL/DML。
- 标记为"需人工复核"的项，报告不代入自动结论，需结合业务语义人工确认。
- 索引"未使用"判断依赖 `pg_stat_user_indexes` 自上次统计重置以来的累计值，重置时间过近会导致误判，已在报告中标注。
