# SQL 上线前审查报告模板

> 复制本模板并填充实际内容，作为最终交付报告。

---

## 总体评估

- **整体风险评分**：X / 10（10 为最高风险）
- **是否建议直接上线**：是 / 否 / 有条件通过
- **上线前提条件**（如有）：
  1. ...
  2. ...

---

## 审查背景

- 目标实例：`<host>:<port>/<dbname>`（版本：`<server_version>`）
- 关键超时参数：`statement_timeout=<...>`、`lock_timeout=<...>`、`idle_in_transaction_session_timeout=<...>`
- 涉及对象数量：`<N>` 个表 / `<M>` 条 SQL
- 调用频率与场景：`<用户提供的频率信息，如"高频 OLTP，QPS ~500" 或 "一次性报表，每周一次">`

---

## 风险发现清单

> 按风险等级排列：🔴 严重 / 🟠 警告 / 🟡 建议 / 🟢 通过

### 🔴 严重

**[SQL #1] `<SQL 文本摘要，不超过一行>`**
- **风险描述**：...
- **可能后果**：...
- **修改建议**：
  ```sql
  -- 可直接使用的修正 SQL
  ```
- **回滚建议**：`<如适用>`
- **高频执行敏感项**：是 / 否

### 🟠 警告

**[SQL #x] `<摘要>`**
- 风险描述 / 可能后果 / 修改建议 / 回滚建议 / 是否高频敏感（同上结构）

### 🟡 建议

**[SQL #x] `<摘要>`**
- 同上结构，程度较轻的优化建议

### 🟢 通过

**[SQL #x] `<摘要>`**
- 简述通过原因（如：已有合适索引、低频调用、已包含超时保护等）

---

## 逐条 SQL 审查详情

> 对每条 SQL 展开七大维度的具体核查结果，可按需省略"通过"的维度以节省篇幅。

### SQL #1

- **类型**：`<SELECT/INSERT/UPDATE/DELETE/DDL/函数/触发器>`
- **执行计划摘要**：`<Seq Scan / Index Scan，预估行数、代价>`
- **索引分析**：...
- **锁与重写风险**：...
- **规范问题**：...
- **回退方案**：...
- **注入风险**：...
- **触发器关联风险**：`<如涉及>`
- **环境关联风险**：`<统计信息、长事务、复制延迟、资源消耗>`

（其余 SQL 依此展开）

---

## 待确认事项

> 若审查过程中信息不足，列出需要用户补充的问题

1. 该 SQL 的调用频率/场景是什么？（高频 OLTP / 一次性报表 / 后台批处理）
2. 应用层是否使用了参数化查询（PreparedStatement）？
3. ...
